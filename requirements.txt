gradio>=6.0
pdfplumber>=0.11
tiktoken>=0.7
httpx>=0.27
pydantic>=2.6
lmdb>=1.4.1
orjson>=3.10
tenacity>=8.2
Pillow>=10
psutil>=5.9

# YouTube upload (optional — only needed if using the Upload to YouTube
# feature in the Podcast Studio). Run scripts/youtube_auth.py once after
# install to do the OAuth dance and cache a refresh token.
google-api-python-client>=2.140
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
